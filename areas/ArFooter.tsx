import styles from '../styles/ArFooter.module.sass'
const {stArFooter} = styles

interface I_Props{}

function ArFooter({}: I_Props) {
	return (
		<footer className={stArFooter}>
			ArFooter
		</footer>
	)
}

export default ArFooter
