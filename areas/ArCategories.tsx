import styles from '../styles/ArCategories.module.sass'
const {stArCategories} = styles

interface I_Props{}

function ArCategories({}: I_Props) {
	return (
		<div className={stArCategories}>
			ArCategories
		</div>
	)
}

export default ArCategories
